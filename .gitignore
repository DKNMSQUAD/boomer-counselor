
# Build tooling caches
# CF Pages builds these fresh on every push via build-all.sh
# Dependencies (not committed - rebuilt during local dev only)
# Editor
# Environment
# Native build output (assembled from hub + tool dists, not web-served)
# Note: apps/*/dist/ IS committed intentionally (pre-built bundles served by Netlify)
# macOS
*.log
*.swp
.DS_Store
.cache/
.env
.env.local
.idea/
.obsidian/
.vite/
.vscode/
apps/*/dist/
apps/*/node_modules/
dist-native/
node_modules/
npm-debug.log*
yarn-debug.log*
